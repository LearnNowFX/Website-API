export enum ServerEvent {
  SERVER_STARTED = "Server started",
  DATABASE_CONNECTED = "Database connected",
  START_GET_ITEM_FROM_CACHE = "Start get item from cache",
  ITEM_DOES_NOT_EXIST_IN_CACHE = "Item does not exist in cache",
  ITEM_RETRIEVED_FROM_CACHE = "Item retrieved from cache",
}
